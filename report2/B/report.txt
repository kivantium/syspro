gcc -o mycp main.c
のようにコンパイルして
./mycp source.txt destination.txt
のように利用する

コピー元、コピー先が同じ際には同じファイルである旨を表示するようにした。
コピー先のパーミッションをコピー元と同じにするようにした。
すでに存在するファイルをコピー先にする場合にはパーミッションが変更されないが、これはLinuxのcpと同じ挙動だったので仕様である。
